#ifndef alx_africa
#define alx _africa

#include "stdio.h"
#include "stdlib.h"
#include "stdout.h"
#include "stdarg.h"

int _printf(const char *format, ...);
const char *s = va_arg(args, const char *);
char c = (char) va_arg(args, int);
const char *ptr = format; *ptr; ++ptr;

#endif
